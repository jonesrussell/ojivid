version: '3'

tasks:
  build:windows:
    desc: Build the application for Windows
    cmds:
    - go build -ldflags "-H windowsgui {{.LDFLAGS}}" -o bin\ojivid.exe main.go

  run:
    desc: Run the application on Windows
    cmds:
    - bin\ojivid.exe
